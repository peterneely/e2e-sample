Given(/^an online customer added a product to their shopping bag$/) do
  pending
end

Then(/^they should be able to increment the quantity by (\d+) with each click$/) do |arg|
  pending
end

And(/^the quantity in the shopping bag summary should increment with each click$/) do
  pending
end

And(/^the price in the product summary should increment with each click$/) do
  pending
end

And(/^the subtotal in the shopping bag summary should increment with each click$/) do
  pending
end

And(/^the FRIES promotion should be shown with each click if a matching promotion is available$/) do
  pending
end

And(/^the FRIES promotion should be hidden with each click if a matching promotion is not available$/) do
  pending
end

Then(/^they should be able to decrement the quantity by (\d+) with each click$/) do |arg|
  pending
end

And(/^the quantity in the shopping bag summary should decrement with each click$/) do
  pending
end

And(/^the price in the product summary should decrement with each click$/) do
  pending
end

And(/^the subtotal in the shopping bag summary should decrement with each click$/) do
  pending
end


And(/^the quantity in the product summary is 99$/) do
  pending
end

Then(/^the increment control should be disabled$/) do
  pending
end

And(/^the quantity in the product summary is 1$/) do
  pending
end

Then(/^the decrement control should be disabled$/) do
  pending
end

Then(/^they should be able to enter a quantity from (\d+) to (\d+), inclusive$/) do |arg1, arg2|
  pending
end

And(/^the quantity in the shopping bag summary should update with each keypress$/) do
  pending
end

And(/^the price in the product summary should update with each keypress$/) do
  pending
end


And(/^the subtotal in the shopping bag summary should update with each keypress$/) do
  pending
end

And(/^the FRIES promotion should be shown with each keypress if a matching promotion is available$/) do
  pending
end

And(/^the FRIES promotion should be hidden with each keypress if a matching promotion is not available$/) do
  pending
end

When(/^they try to enter an (.*)$/) do |invalid_quantity|
  pending
end

Then(/^the quantity in the product summary should change to (.*)$/) do |value|
  pending
end

And(/^the quantity in the shopping bag summary should change to (.*)$/) do |value|
  pending
end

And(/^the price in the product summary should update$/) do
  pending
end

And(/^the subtotal in the shopping bag summary should update$/) do
  pending
end

And(/^the FRIES promotion should be shown if a matching promotion is available$/) do
  pending
end

And(/^the FRIES promotion should be hidden if a matching promotion is not available$/) do
  pending
end

And(/^customer should see an error message$/) do
  pending
end